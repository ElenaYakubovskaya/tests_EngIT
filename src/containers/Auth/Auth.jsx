import React, { Component } from 'react';
import styled from 'styled-components';

const StyleAuth = styled.div`
  margin-left: 500px;
`;

export default class Auth extends Component {
  render() {
    return <StyleAuth>Auth</StyleAuth>;
  }
}
